# Pullframe 1.0.1

This hotfix corrects the macOS signed app bundle so the bundled yt-dlp runtime can load videos in the production build.

## Fixes

- Fixed bundled yt-dlp startup in the signed and notarized macOS app.
- Restored the expected first-run smoke flow for loading YouTube video details before activation.

